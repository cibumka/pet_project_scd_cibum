name: pet_dwh
version: "1.0.0"
config-version: 2

profile: pet_dwh

model-paths: ["models"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  pet_dwh:
    +file_format: iceberg
    mart:
      +materialized: table

snapshots:
  pet_dwh:
    +file_format: iceberg
